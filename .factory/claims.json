[
  {
    "id": "sample-match-end",
    "claim": "The sample match reaches an actual win, loss, or draw screen.",
    "where": "Demo game and README",
    "test": "npm test -- --grep @claim:sample-match-end",
    "sandbox": "Fresh browser context at /demo; queue the supplied sample plan until the end screen appears."
  },
  {
    "id": "restart-reset",
    "claim": "Play the sample again resets the board, round, queue, and integrity.",
    "where": "Sample end screen",
    "test": "npm test -- --grep @claim:restart-reset",
    "sandbox": "Finish the deterministic sample, restart it, and inspect the observable round and craft state."
  },
  {
    "id": "settings-persist",
    "claim": "Game settings persist on this device and demo settings use a separate namespace.",
    "where": "Settings dialog and privacy page",
    "test": "npm test -- --grep @claim:settings-persist",
    "sandbox": "Fresh /demo context; change sound, reload, inspect the control and localStorage namespaces."
  },
  {
    "id": "steady-frame-rate",
    "claim": "The sample animation loop sustains at least 55 frames per second in the phone test.",
    "where": "README and handoff",
    "test": "npm test -- --grep @claim:steady-frame-rate",
    "sandbox": "Chromium at 390x844; measure requestAnimationFrame for 2.2 seconds during the sample match."
  },
  {
    "id": "online-match-duration",
    "claim": "A full six-round online match gives each round a 20-second planning window, for about 2 minutes of decision time.",
    "where": "README",
    "test": "npm test -- --grep @claim:online-match-duration",
    "sandbox": "Create an isolated local room, complete six rounds, and measure each advertised planning deadline."
  },
  {
    "id": "demo-private",
    "claim": "The sample uses a separate namespace and loads no analytics, ads, trackers, external fonts, or third-party scripts.",
    "where": "Landing facts, demo banner, privacy page, and README",
    "test": "npm test -- --grep @claim:demo-private",
    "sandbox": "Fresh browser context; record all requests during one sample round and inspect localStorage."
  },
  {
    "id": "online-network-private",
    "claim": "Online play contacts only the Signal Salvo site and its product-owned room service.",
    "where": "Landing privacy section, privacy page, and README",
    "test": "npm test -- --grep @claim:online-network-private",
    "sandbox": "Fresh browser context; record every request while creating a real local room and assert only the static site and local room service origins appear."
  },
  {
    "id": "no-personal-data-storage",
    "claim": "The room service does not store names, email addresses, or account details.",
    "where": "Privacy page",
    "test": "npm test -- --grep @claim:no-personal-data-storage",
    "sandbox": "Start an isolated real room service and SQLite file; submit unique personal-data markers through room requests, then verify neither responses nor the persisted database contain them."
  },
  {
    "id": "request-log-privacy",
    "claim": "Room-service logs contain request paths and status codes, but not reconnect tokens or command bodies.",
    "where": "Privacy page",
    "test": "npm test -- --grep @claim:request-log-privacy",
    "sandbox": "Start an isolated real room service with captured logs; send an authenticated command request and verify path and status entries exist while the token and unique command-body markers do not."
  },
  {
    "id": "online-two-player",
    "claim": "Two independent browser clients can finish a room while plans stay hidden until both lock.",
    "where": "Landing page, game planner, and README",
    "test": "npm test -- --grep @claim:online-two-player",
    "sandbox": "Two isolated Chromium contexts use the local Rust service to create, join, play six rounds, and reach both end screens."
  },
  {
    "id": "room-reconnect",
    "claim": "Either player can reload during a match and recover the same private room state.",
    "where": "README",
    "test": "npm test -- --grep @claim:room-reconnect",
    "sandbox": "Two isolated Chromium contexts join a room; one locks a plan, both reload, and each recovers only its own view."
  },
  {
    "id": "online-rematch",
    "claim": "A finisher can create a fresh rematch room from the result screen.",
    "where": "Online result screen and README",
    "test": "npm test -- --grep @claim:online-rematch",
    "sandbox": "Two isolated Chromium contexts finish a room; one selects Create a rematch and receives a different room code."
  },
  {
    "id": "room-token-expiry",
    "claim": "Each reconnect token stops working after its player receives the match result.",
    "where": "Privacy page and README",
    "test": "npm test -- --grep @claim:room-token-expiry",
    "sandbox": "Two API clients complete six rounds, receive final views, then verify both tokens return HTTP 410."
  },
  {
    "id": "free-no-account",
    "claim": "A visitor can create a free room without an account, payment step, or download.",
    "where": "Landing page and terms page",
    "test": "npm test -- --grep @claim:free-no-account",
    "sandbox": "Fresh browser context; verify no credential or download flow, create a room, and inspect the resulting path."
  },
  {
    "id": "service-health",
    "claim": "The room service health endpoint reports its running build.",
    "where": "README",
    "test": "npm test -- --grep @claim:service-health",
    "sandbox": "Start the local Rust service, request /health, and assert a healthy response with a concrete build identifier."
  },
  {
    "id": "room-restart-persistence",
    "claim": "An active room and its locked plan survive a room-service restart.",
    "where": "README and terms page",
    "test": "npm test -- --grep @claim:room-restart-persistence",
    "sandbox": "Create and join a room on an isolated Rust service, lock one plan, restart the real process on the same SQLite file, and finish the round."
  }
]
